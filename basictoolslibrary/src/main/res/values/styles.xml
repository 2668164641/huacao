<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SpotsDialogDefault" parent="android:Theme.DeviceDefault.Light.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogTitleText">Loading…</item>
        <item name="DialogSpotCount">5</item>
    </style>
    <!-- Base application theme. -->
    <style name="Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- ToolBar背景色 -->
        <!--<item name="colorPrimary">#1793FD</item>-->
        <!-- 状态栏背景色 -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryColor</item>
        <!-- checkbox等控件的选中颜色 -->
        <!-- 波纹颜色 -->
        <item name="colorControlHighlight">#e7e6e6</item>
    </style>

    <style name="AlertDialogTheme" parent="android:Theme.DeviceDefault.Dialog">
        <!-- ToolBar背景色 -->
        <!--<item name="colorPrimary">#1793FD</item>-->
        <!-- 状态栏背景色 -->
        <item name="android:textColor">@color/primaryColor</item>
        <item name="android:textSize">14sp</item>
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryColor</item>
    </style>

    AlertDialog.THEME_HOLO_DARK
    <style name="SwipeBackLayout">
        <item name="edge_size">50dip</item>
        <item name="shadow_left">@drawable/toolslib_shadow_left</item>
        <item name="shadow_right">@drawable/toolslib_shadow_right</item>
        <item name="shadow_bottom">@drawable/toolslib_shadow_bottom</item>
    </style>
    <style name="bottomdialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
    </style>

    <style name="mydialogstyle" parent="android:style/Theme.Dialog">
        <item name="@android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 边框 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>